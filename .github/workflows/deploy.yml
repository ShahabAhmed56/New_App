name: Daily Production Deployment

on:
  schedule:
    # 2:40 PM Pakistan Time (UTC+5) = 09:40 UTC
    - cron: "00 10 * * *"
  workflow_dispatch:

concurrency:
  group: koyeb-prod
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest main
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Install Koyeb CLI
        run: |
          set -e
          curl -fL https://github.com/koyeb/koyeb/releases/latest/download/koyeb-linux-amd64 -o koyeb
          chmod +x koyeb
          sudo mv koyeb /usr/local/bin/koyeb

      - name: Verify Koyeb CLI
        run: koyeb version

      - name: Deploy to Koyeb
        env:
          KOYEB_TOKEN: ${{ secrets.KOYEB_TOKEN }}
        run: |
          koyeb service redeploy new-app
